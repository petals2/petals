var output: list<number> = [ 0 ]

class Rose {
  protected x: number;

  constructor(balls: number) {
    this.x = balls;
  }
}

class Vector2 {
    protected x: &Rose;

    constructor(initX: &Rose) { this.x = initX }

    getX(): &Rose {
      return this.x;
    }
}

var rose = &new Rose(5);
var s = new Vector2(rose);
var y = s.getX();

output.push(y.x);
